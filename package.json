{
  "name": "text-extract",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "author": "Gillis Werrebrouck",
  "repository": {
    "type": "git",
    "url": ""
  },
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "test": "ava -T 20s --verbose test/*.test.js"
  },
  "dependencies": {
    "@google-cloud/pubsub": "0.22.2",
    "@google-cloud/storage": "^2.3.3",
    "@google-cloud/translate": "2.1.3",
    "@google-cloud/vision": "0.24.0",
    "safe-buffer": "5.1.2"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "3.1.0",
    "ava": "0.25.0",
    "proxyquire": "2.1.0",
    "sinon": "7.2.3"
  },
  "cloud-repo-tools": {
    "requiresKeyFile": true,
    "requiresProjectId": true
  },
  "description": "[Google cloud tutorial](https://cloud.google.com/functions/docs/tutorials/ocr#functions_ocr_deploy_process-node8)\r ## Deployment\r * [Create](https://console.cloud.google.com/cloud-resource-manager) Google Cloud project\r * Enable billing in your account to be able to use most Google Cloud service\r * [Enable](https://console.cloud.google.com/flows/enableapi?apiid=cloudfunctions,pubsub,storage_api,translate,vision.googleapis.com) APIS for the project (Cloud Functions API, Cloud Pub/Sub API, Google Cloud Storage JSON API, Cloud Translation API and Cloud Vision API)\r * [Install](https://cloud.google.com/sdk/) the gcloud SDK\r * Initialize gcloud: `gcloud init`\r * Update gcloud and install beta components: `gcloud components update && gcloud components install beta`\r * Create an input and output bucket: `gsutil mb gs://*YOUR_BUCKET_NAME*`\r * Configure the app in config.json\r * Deploy image processing function: `gcloud functions deploy ocr-extract --runtime nodejs8 --trigger-bucket *YOUR_IMAGE_BUCKET_NAME* --entry-point processImage`\r * Deploy text translation function: `gcloud functions deploy ocr-translate --runtime nodejs8 --trigger-topic *YOUR_TRANSLATE_TOPIC_NAME* --entry-point translateText`\r * Deploy save results function: `gcloud functions deploy ocr-save --runtime nodejs8 --trigger-topic *YOUR_RESULT_TOPIC_NAME* --entry-point saveResult`\r * Test the service via cmd: `gsutil cp *PATH_TO_IMAGE* gs://*YOUR_IMAGE_BUCKET_NAME*`\r * Get last logs: `gcloud functions logs read --limit 100`\r ## Test the code\r `npm test`",
  "main": "index.js",
  "directories": {
    "test": "test"
  }
}
